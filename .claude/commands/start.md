# /start — Load Project Context

Read these files in order before doing anything:

1. `AGENTS.md`
2. `CLAUDE.md`
3. `docs/FRS.md`
4. `docs/SDS.md`

After reading, confirm with:

> "Ready. Loaded FRS, SDS, AGENTS.md, and CLAUDE.md.
> Tech stack: React + Zustand + TypeScript (frontend), Node + Express + PostgreSQL (backend).
> Monolithic structure. Spec-driven workflow active.
> What are we building?"

Do NOT start any implementation until given a ticket or explicit task.
